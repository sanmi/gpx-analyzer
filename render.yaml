services:
  - type: web
    name: route-estimator
    runtime: python
    buildCommand: pip install -e .
    startCommand: gunicorn gpx_analyzer.web:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
