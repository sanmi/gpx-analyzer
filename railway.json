{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install -e . && gunicorn gpx_analyzer.web:app --bind 0.0.0.0:$PORT"
  }
}
